Server commands

authored by ting sen and guan jie
code uses commons-lang3-3.4 library 'split' function to split strings into tokens seperated by spaces.
command is recognized by first token in a string.

1. login [username] [password] - login
2. logout / quit - logoff
3. msg [username] [message] - send message to username